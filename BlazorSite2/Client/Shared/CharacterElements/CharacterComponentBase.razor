@using BlazorSite2.Shared.Arklens;

@code {
	protected override void OnParametersSet()
		=> Character.PropertyChanged += (_, _) => this.StateHasChanged();
#pragma warning disable
	[Parameter]
	public Character Character { get; set; }
#pragma warning restore
}
